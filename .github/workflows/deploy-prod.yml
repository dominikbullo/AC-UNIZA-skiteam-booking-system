name: Deploy to staging server

on:
  workflow_run:
    workflows: ["Build & Publish to GitHub Container Registry"]
    branches: [master]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to staging server
        uses: garygrossgarten/github-action-ssh@release
        with:
          command: |
            cd ${{ secrets.PROJECT_DIR }}
            git reset --hard HEAD
            git pull
            docker-compose -f production.yml up -d --build --force-recreate --no-deps
          host: ${{ secrets.HOST }}
          username: ${{ secrets.HOST_USERNAME }}
          passphrase: ${{ secrets.PASSPHRASE }}
          privateKey: ${{ secrets.PRIVATE_KEY}}
